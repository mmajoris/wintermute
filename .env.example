# =============================================================================
# Wintermute / nulltruth.com Environment Configuration
# =============================================================================

# Brain Events API Authentication
# Shared secret between Molly and this app
# Generate: openssl rand -hex 32
BRAIN_API_KEY=your-secure-api-key-here

# NextAuth.js Configuration
# Generate: openssl rand -base64 32
AUTH_SECRET=your-nextauth-secret-here

# Production URL (required for NextAuth in production)
AUTH_URL=https://nulltruth.com

# User Authentication
# The only email allowed to log in
AUTH_ALLOWED_EMAIL=admin@nulltruth.com

# Password hash (bcrypt, base64 encoded)
# 1. Generate hash: node scripts/hash-password.js yourpassword
# 2. Base64 encode: echo -n '$2b$10$...' | base64
AUTH_PASSWORD_HASH_B64=your-base64-encoded-bcrypt-hash

# Optional: IP allowlist (comma-separated)
# AUTH_ALLOWED_IPS=192.168.1.100,10.0.0.1
