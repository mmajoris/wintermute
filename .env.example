# Brain Events API Authentication
# Generate a secure random key for Molly to use when pushing events
# Example: openssl rand -hex 32
BRAIN_API_KEY=your-secure-api-key-here

# NextAuth.js Configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-nextauth-secret-here

# User Authentication
# The only email allowed to log in
AUTH_ALLOWED_EMAIL=admin@wintermute.local

# Password hash (bcrypt)
# Generate with: node scripts/hash-password.js yourpassword
# Base64 encoded bcrypt hash - use: echo -n 'your_bcrypt_hash' | base64
AUTH_PASSWORD_HASH_B64=$2b$10$...your-bcrypt-hash-here...

# Optional: IP allowlist (comma-separated)
# AUTH_ALLOWED_IPS=192.168.1.100,10.0.0.1

# Auth URL (for production)
# AUTH_URL=https://your-domain.com
